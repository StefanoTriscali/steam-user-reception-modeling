# Models

This directory is reserved for trained model artifacts.

The current version of the project focuses on the full modeling workflow inside the notebook. Future versions may include serialized model artifacts and inference scripts.
